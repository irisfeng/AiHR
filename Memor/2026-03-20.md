# AIHR 开发记录 - 2026-03-20

## 今日目标

- 建立 AIHR 项目基础仓库
- 明确 1 周 MVP 范围
- 搭好第一版 Frappe 自定义 app 骨架

## 今日完成

- 初始化本地 Git 仓库并连接远端 GitHub 仓库
- 创建并推送首个提交：`9c54b0c Initial AIHR MVP scaffold`
- 完成中文版项目说明文档
- 新增 7 天执行计划和 `Memor/` 日志机制
- 新建 `aihr` 自定义 app 基础目录结构
- 完成招聘流程第一版扩展字段
  - `Job Requisition`
  - `Job Opening`
  - `Job Applicant`
  - `Interview`
  - `Job Offer`
- 新增 `AI Screening` DocType
- 实现简历解析服务
- 实现启发式候选人筛选服务
- 新增本地开发环境接入文档
- 新增 `bootstrap_frappe_docker.sh` 开发辅助脚本
- 新增 demo 招聘数据生成入口
- 新增本地预览脚本和基础测试
- 本地 `.frappe_docker` 参考环境已初始化
- `.frappe_docker/development/apps.json` 已写入 ERPNext + HRMS 依赖
- 已成功创建 `development.localhost` 本地开发站点
- 已修复 `aihr` bench 安装链路并完成真实安装
- 已验证 `AI Screening` DocType 和招聘扩展字段已同步到站点
- 已启动本地开发 Web 服务并确认 `http://development.localhost:18000` 可访问
- 已成功灌入演示数据
  - 1 条 `Job Requisition`
  - 1 条 `Job Opening`
  - 3 条 `Job Applicant`
  - 3 条 `AI Screening`
- 新增本地联调辅助脚本
  - `scripts/start_dev_site.sh`
  - `scripts/seed_demo_site.sh`
- 新增项目北极星文档：`docs/north-star.md`
- 新增招聘自动化逻辑
  - `Job Requisition` 保存时自动生成 `Agency Brief`
  - `Job Opening` 保存时自动同步 `Agency Pack`
  - `Job Applicant` 新增或简历更新时自动触发 AI 初筛
- 新增招聘自动化辅助服务和测试
  - `aihr/services/recruitment_ops.py`
  - `tests/test_recruitment_ops.py`
- 新增 Client Script 注入机制
  - `Job Applicant` 候选人摘要卡与“运行 AI 初筛”按钮
  - `Job Requisition` 代理发布包刷新/复制按钮
  - `Job Opening` 批量初筛、发布包同步、候选人概览按钮
- 完成浏览器级烟测
  - 候选人详情页已展示 AI 摘要卡
  - 岗位需求页与岗位开启页自定义操作按钮已渲染
- AI 初筛文案改为中文优先
  - 摘要、优势、风险、追问问题已切换为中文表达
  - 代理发布包已切为中文格式
- 增强中文简历解析
  - 支持中文姓名、城市、年限与技能词识别
  - 收紧中文技能提取，避免把整句职责误识别为技能
- 修复 bench 软链 app 的前端资源识别
  - `scripts/install_local_app.sh` 现在会跟随软链并正确触发 `bench build`

## 今日关键决策

- 一期不做完整 HRMS，只做招聘优先的 AIHR MVP
- 使用 Frappe + ERPNext + HRMS 作为底座
- 不深度 fork `frappe/hrms`，而是通过 `aihr` 自定义 app 二开
- 开发环境优先采用 Frappe Docker 方案
- 本周优先目标是先跑通招聘主链路，不碰复杂薪资计算

## 当前代码范围

- `README.md`
- `docs/week-1-mvp.md`
- `docs/data-model.md`
- `docs/week-1-execution-plan.md`
- `docs/development-setup.md`
- `aihr/setup/custom_fields.py`
- `aihr/setup/demo_data.py`
- `aihr/api/recruitment.py`
- `aihr/api/demo.py`
- `aihr/services/resume_parser.py`
- `aihr/services/screening.py`
- `aihr/recruitment_intelligence/doctype/ai_screening/`
- `scripts/bootstrap_frappe_docker.sh`
- `scripts/install_local_app.sh`
- `scripts/preview_screening.py`

## 当前验证结果

- `python3 -m unittest discover -s tests` 通过
- 本地 `preview_screening.py` 已成功生成候选人摘要卡样例
- 远端仓库已推送成功
- `bootstrap_frappe_docker.sh` 已成功拉起本地 Frappe Docker 参考目录
- `scripts/install_local_app.sh` 已成功安装 `aihr` 到 `development.localhost`
- 本地浏览器入口已可访问：`http://development.localhost:18000`
- demo 招聘数据已成功写入站点
- `python3 -m unittest discover -s tests` 通过，当前共 6 条测试
- `python3 -m unittest discover -s tests` 通过，当前共 8 条测试
- 岗位需求字段更新后，代理发布包已能自动刷新
- 新增候选人后，`AI Screening` 已能自动生成
- `screen_job_opening_applicants` 批量初筛接口已验证可用
- Playwright 浏览器烟测通过
  - 候选人页已显示中文状态标签“待经理复核”
  - AI 摘要卡已显示中文摘要、风险与追问问题
  - `Job Opening` 页已显示“同步代理发布包 / 批量 AI 初筛 / 查看候选人概览”
  - `Job Requisition` 页已显示“刷新代理发布包 / 复制代理发布包”

## 当前风险与阻塞

- 还缺真实岗位样本和真实简历样本用于联调
- 当前仍基于标准 Frappe 表单，专用“招聘作战台”首页还未开始做
- 实时服务目前未完整拉起，浏览器会看到 `socket.io` 404，但不影响当前 MVP 主链路演示

## 明日计划 - 2026-03-21

- 开始做招聘主链路页面和表单体验优化
- 引入真实岗位样本和真实简历样本联调
- 优先补岗位发布包、候选人详情卡和面试协同动作
- 准备 1 个真实岗位和若干测试简历作为联调样本
- 继续收敛中文默认体验，但底层字段和状态码保持英文，方便后续双语扩展
