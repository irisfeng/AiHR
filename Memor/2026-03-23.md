# 2026-03-23

## 今日完成

- 继续推进权限治理 P0，围绕“交付中心经理可直接进入并查看本部门招聘数据”做了一轮真实联调。
- 修复了交付中心演示数据灌数过程中的时间戳冲突：
  - 问题表现：`Job Applicant` 在自动筛选钩子更新后，再被 demo 灌数逻辑使用旧对象保存，触发 `TimestampMismatchError`
  - 修复方式：`_screen_applicant` 在保存前重新获取最新 `Job Applicant` 文档，并在筛选后返回最新对象
  - 涉及文件：`aihr/setup/demo_data.py`
- 将交付中心招聘样本正式接入 demo 数据链路：
  - 岗位需求单：`HR-HIREQ-00004`
  - 招聘中岗位：`HR-OPN-2026-0004`
  - 岗位名称：`交付工程师 - AIHR Demo`
  - 部门：`交付中心 - AD`
- 成功灌入交付中心候选人样本：
  - `delivery.chenhan@aihr.local`
  - `delivery.lixiang@aihr.local`
  - `delivery.zhouning@aihr.local`
- 成功生成交付中心 AI 初筛结果：
  - `AI-SCR-00028`
  - `AI-SCR-00029`
  - `AI-SCR-00030`
- 使用真实站点查询确认交付中心数据已落库：
  - `Job Opening`：`1` 条
  - `Job Applicant`：`3` 条
  - `AI Screening`：`3` 条
- 使用真实浏览器登录 `delivery.manager@aihr.local` 完成角色入口与页面联调：
  - 登录后正确进入 `/app/aihr-manager-review`
  - 页面标题：`AIHR 用人经理中心`
  - 页面中可见：
    - `3` 条待复核
    - `3` 位候选人
    - `1` 场面试
    - `1` 条 Offer
- 核查确认：
  - 交付中心经理不再被错误带到 `AIHR 招聘总览`
  - 交付中心经理当前能看到的确实是本部门招聘样本，而不是人事部演示数据
- 正式落地 `Job Requisition` 审批流：
  - 新增 `AIHR Job Requisition Approval`
  - Workflow 状态：
    - `草稿`
    - `待HR处理`
    - `已批准`
    - `已驳回`
    - `已暂停`
    - `已完成`
    - `已取消`
  - Workflow 动作：
    - `提交需求`
    - `批准开启`
    - `退回修改`
    - `重新提交`
    - `暂停招聘`
    - `恢复招聘`
    - `取消需求`
- 新增 Workflow 初始化链路：
  - `aihr/setup/workflows.py`
  - `install.py` 与 `after_migrate` 已接入 `ensure_aihr_workflows`
- `Job Requisition` 页面已同步收口：
  - `status` 改为只读
  - 未批准前不再展示“新建招聘中岗位”
  - 即使已批准，也只有 `HR Manager / System Manager` 才会看到“新建招聘中岗位”
- 修复一个历史状态迁移问题：
  - `workflow_state` 字段由 Workflow 自动创建为 `Custom Field`
  - 之前误用 `DocField` 判断，导致旧岗位需求未能从 `草稿` 回填到正确审批状态
  - 现已修正，并成功将现有 `Open & Approved` 需求映射为 `已批准`
- 补做真实 Workflow 验证：
  - 新建交付中心测试需求后，经理可见动作为：`提交需求`、`取消需求`
  - 现有已批准需求在管理员视角下可见动作为：`暂停招聘`、`取消需求`
  - 已移除重复动作，避免管理员因多角色看到双份按钮

## 联调与验证

- `python3 -m unittest discover -s tests`：`70` 条测试通过。
- `python3 -m compileall aihr tests`：通过。
- 站点真实灌数返回：
  - `delivery_job_requisition = HR-HIREQ-00004`
  - `delivery_job_opening = HR-OPN-2026-0004`
  - `delivery_job_applicants = delivery.chenhan@aihr.local, delivery.lixiang@aihr.local, delivery.zhouning@aihr.local`
- 真实浏览器验证结果：
  - 账号：`delivery.manager@aihr.local`
  - 默认落点：`/app/aihr-manager-review`
  - 当前页面已展示交付中心经理视角统计数据
- 接入审批流后再次回归：
  - `python3 -m unittest discover -s tests`：`75` 条测试通过
  - `python3 -m compileall aihr tests`：通过
- 站点 `bench migrate`：通过
  - 站点内已确认：
    - `Workflow = AIHR Job Requisition Approval`
    - `Job Requisition.workflow_state` 字段已存在
    - 历史 `Open & Approved` 需求已映射为 `已批准`
- 启动一轮招聘 MVP 收口：
  - `AIHR 招聘总览` 主工作台已改成招聘核心视角
  - 首页快捷入口改成：`新建岗位需求 / 导入简历包 / 候选人池 / AI 初筛 / 面试安排 / Offer 管理`
  - 主工作台链接已移除员工、入职、用户、部门等非招聘核心入口
  - 首页 Hero、角色卡和路径说明已改成“ZIP 简历导入 -> AI 初筛 -> 面试 -> Offer”口径
  - 登录页文案已同步收口到招聘核心流程，并更新登录资源版本号避免浏览器继续命中旧文案
  - `README.md` 已改成招聘 MVP 说明，不再把入职交接当作一期主链路
  - `docs/user-manual.md` 已重写为招聘版手册，只保留招聘核心工作流
  - `docs/hr-roles-process-assessment.md` 已同步修正，不再把入职/薪酬当作近期重点
- `AI Screening` 页面已改成“候选人 AI 决策页”：
  - 顶部显示候选人、岗位名称、状态、匹配分
  - 左侧收口为 `AI 摘要 / 匹配技能 / 缺失技能 / 推荐理由 / 风险点 / 建议追问`
  - 右侧直接预览原始简历电子版
  - 原始 `JSON` 调试信息默认隐藏，仅通过“查看解析调试”手动展开
- 真实 PDF 预览联调：
  - `AI-SCR-00025` 已确认能直接预览 `陈寒简历.pdf`
  - 之前直接嵌入 `/private/files/...` 会触发 `403 Forbidden`
  - 已新增带权限校验的站内预览接口，避免私有附件在 iframe 内直接失效
- 这轮结论：
  - `AI 初筛` 页面现在更接近经理复核页，而不是系统原始表单页
  - 左侧系统侧栏仍有进一步弱化空间，但主链路信息已经从“细碎字段堆叠”收成“预览 + 摘要 + 决策”结构

## 下一步

- 继续推进招聘 MVP 收口：隐藏或弱化更多非招聘主线入口。
- 为交付中心经理补充更完整的“导入结果页 / 候选人摘要页 / 待经理复核路径”联调。
- 开始梳理“经理只能看本人负责岗位”这一层比部门更细的范围控制。
