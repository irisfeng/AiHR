# 2026-03-21

## 今日完成

- 打通招聘主链路后半段，新增 `Interview` 和 `Job Offer` 的 AIHR 表单增强。
- 为 `Interview` 新增 `AIHR Interview Control Room` 面板，支持：
  - 面试官资料包生成
  - 跟进动作同步
  - 候选人摘要直达
- 为 `Job Offer` 新增 `AIHR Offer Handoff` 面板，支持：
  - 入职交接准备
  - 薪酬交接状态更新
  - 候选人摘要直达
- 在候选人页新增 `安排面试`、`生成 Offer` 快捷动作。
- 为 `Interview` / `Job Offer` 增加自动同步事件：
  - 保存面试时自动回写候选人当前阶段和下一步动作
  - 保存 Offer 时自动回写候选人阶段和交接动作
- demo 数据扩展到后半链路，新增：
  - `Interview Round`
  - `Interview`
  - `Job Offer`
- 更新使用手册到当前能力边界。
- 工作台中的 `Job Offer`、`Employee Onboarding` 等入口文案改为中文业务语言。
- 继续补完 `Interview Feedback` 与 `Employee Onboarding` 两个关键闭环页面。
- 为 `Interview Feedback` 新增 `AIHR Feedback Console` 面板，支持：
  - 自动加载评分项
  - 面试结论同步
  - 候选人 AI 摘要联动
- 为 `Employee Onboarding` 新增 `AIHR Onboarding Hub` 面板，支持：
  - 一键准备入职清单
  - 负责人 / 薪酬就绪状态汇总
  - 预入职说明与活动清单展示
- demo 数据已扩展到：
  - `Interview Feedback`
  - `Employee Onboarding`

## 关键决策

- 继续坚持“英文技术底座 + 中文业务体验”的路径，先做流程闭环，不把时间优先花在完整国际化上。
- 面试和 Offer 的状态推进以候选人为主线，自动同步回 `Job Applicant`，避免 HR 重复录入。
- `Employee Onboarding` 和正式 Payroll 联动暂不强行接入本周 MVP，先把交接准备和 handoff 信息收口。
- 面试反馈支持草稿即回写主流程，提交后再作为正式归档，兼顾演示效率和真实流程。

## 联调与验证

- `python3 -m unittest discover -s tests` 已扩展到 `17` 条测试并全部通过。
- `python3 -m compileall aihr scripts` 通过。
- 本地站点已重新迁移并可访问：`http://development.localhost:18000`
- demo 数据已成功灌入，当前可直接看到：
  - `HR-HIREQ-00001`
  - `HR-OPN-2026-0001`
  - `HR-INT-2026-0001`
  - `HR-INT-FEED-0001`
  - `HR-OFF-2026-00001`
  - `HR-EMP-ONB-2026-00001`
- 使用 Playwright 做了真实浏览器验证：
  - 面试页能看到 `AIHR Interview Control Room`
  - Offer 页能看到 `AIHR Offer Handoff`
  - 面试反馈页能看到 `AIHR Feedback Console`
  - 入职交接页能看到 `AIHR Onboarding Hub`
  - `准备入职交接`、`生成面试官资料包` 动作均能执行

## 发现并解决的问题

- `prepare_job_offer_handoff` / `prepare_interviewer_pack` 在按钮调用时出现 `TimestampMismatchError`。
- 根因是 `validate` 钩子已自动回写候选人状态，API 又额外保存了一次候选人。
- 处理方式：
  - 保留 `validate` 钩子作为统一状态同步入口
  - 去掉 API 内重复的候选人保存
- 重灌 demo 数据时，原生 `Interview.Scheduled On` 因只写一次约束无法直接改动。
- 处理方式：
  - seed 逻辑改为“老记录不强改排期，只补必要字段”
  - 面试反馈提交失败时保留草稿，避免整批 demo 灌数回滚

## 当前已知问题

- 本地仍然有 `socket.io 404` 控制台报错，因为实时服务层没有一起拉起。
- 该问题不影响当前 MVP 的页面录入、按钮动作和演示流程。
- 员工主档自动创建和 Payroll 正式联动还未完成。

## 下一步

- 继续推进中文优先的表单标签和 Workspace 文案。
- 补 `Employee` 主档自动创建入口，衔接 `Employee Onboarding`。
- 视时间评估是否顺手接入更轻量的实时服务或屏蔽本地 `socket.io` 噪音。
