# 2026-03-21

## 今日完成

- 将默认 Frappe/ERP 壳 OEM 成 AIHR 品牌后台：
  - 登录页改为 `AIHR 登录`
  - 顶部 Logo、品牌名、默认首页切到 AIHR
  - 左侧导航收敛为 AIHR 工作台，不再默认暴露会计/库存等 ERP 模块
- 新增全局 Desk / Login 品牌资源：
  - `aihr/public/css/aihr_desk.css`
  - `aihr/public/css/aihr_login.css`
  - `aihr/public/js/aihr_desk.js`
  - `aihr/public/js/aihr_login.js`
- 新增品牌化安装逻辑：
  - 自动设置 `System Settings` / `Website Settings` / `Navbar Settings`
  - 自动重置默认 `app` 和默认 `workspace`
  - 自动隐藏非 AIHR Public Workspace
- 将 AIHR 工作台扩展为角色入口：
  - `AIHR 招聘总览`
  - `AIHR 用人经理中心`
  - `AIHR 面试协同中心`
- 统一收敛 AIHR 的命名语气：
  - 将偏“作战台 / 控制室 / Console / Hub” 的命名改成更克制的 `总览 / 中心 / 概览`
  - 首页、角色工作台、面试页、反馈页、Offer 页、入职页、登录页和使用手册都已同步更新
- 首页工作台新增角色卡片，支持从首页直达经理中心和面试中心。
- 修正角色工作台路由：
  - Frappe 侧边栏默认生成的中文 slug 无法正确打开新 Workspace
  - 已在 AIHR Desk 壳中统一重写为可用的英文路由
- 修正默认首页路由：
  - 工作台标题中文化后，Frappe 默认会尝试跳转到中文 slug
  - 已将登录后的默认入口固定回稳定可用的 `/app/aihr-hiring-hq`
  - 同时保留中文显示名，不影响用户看到的界面语言
- 修正经理中心统计口径：
  - 将 `待复核候选人` 调整为 `待经理复核`
  - 统计来源改为 `AI Screening.status = Ready for Review`，与招聘总览保持一致
- 修正网站入口收口：
  - 网站侧如 `/projects` 这类 ERP/Portal 路由不再继续展示原始页面
  - 游客会自动跳到 `AIHR 登录`
  - 已登录系统用户会自动回到 `AIHR 招聘总览`
- 启动中文国际化基础设施第一版：
  - 新增 `aihr/translations/zh.csv`
  - 覆盖自定义字段、状态词、`AI Screening` DocType 关键英文标签
  - 在安装逻辑中开始把 `System Settings.language` 和默认英文用户语言收口到 `zh`
  - 追加 Desk 层显示名覆盖，收口 `AI Screening`、`AIHR Hiring HQ` 等内部英文名
- 继续扩展中文国际化到真实使用页：
  - 补齐 `Job Requisition`、`AI Screening` 等核心页面的列表页、表单页、按钮、面包屑、筛选器、状态文案
  - 为 `New ...` / `Add ...` / `List View` / `Clear all filters` / `Naming Series` / `Timelines` / `Posting Date` 等高频 UI 词条补正式中文资源
  - 扩展 Desk 层前端兜底翻译，从纯文本节点覆盖到 `title`、`aria-label`、`placeholder` 等属性级文案
  - 新增 `tests/test_translations.py`，对招聘主链路中文词条做回归校验
- 打通招聘主链路后半段，新增 `Interview` 和 `Job Offer` 的 AIHR 表单增强。
- 为 `Interview` 新增 `AIHR 面试概览` 面板，支持：
  - 面试官资料包生成
  - 跟进动作同步
  - 候选人摘要直达
- 为 `Job Offer` 新增 `AIHR 录用概览` 面板，支持：
  - 入职交接准备
  - 薪酬交接状态更新
  - 候选人摘要直达
- 在候选人页新增 `安排面试`、`生成 Offer` 快捷动作。
- 为 `Interview` / `Job Offer` 增加自动同步事件：
  - 保存面试时自动回写候选人当前阶段和下一步动作
  - 保存 Offer 时自动回写候选人阶段和交接动作
- demo 数据扩展到后半链路，新增：
  - `Interview Round`
  - `Interview`
  - `Job Offer`
- 更新使用手册到当前能力边界。
- 工作台中的 `Job Offer`、`Employee Onboarding` 等入口文案改为中文业务语言。
- 继续补完 `Interview Feedback` 与 `Employee Onboarding` 两个关键闭环页面。
- 为 `Interview Feedback` 新增 `AIHR 反馈概览` 面板，支持：
  - 自动加载评分项
  - 面试结论同步
  - 候选人 AI 摘要联动
- 为 `Employee Onboarding` 新增 `AIHR 入职概览` 面板，支持：
  - 一键准备入职清单
  - 负责人 / 薪酬就绪状态汇总
  - 预入职说明与活动清单展示
- demo 数据已扩展到：
  - `Interview Feedback`
  - `Employee Onboarding`

## 关键决策

- 继续坚持“英文技术底座 + 中文业务体验”的路径，先做流程闭环，不把时间优先花在完整国际化上。
- 面试和 Offer 的状态推进以候选人为主线，自动同步回 `Job Applicant`，避免 HR 重复录入。
- `Employee Onboarding` 和正式 Payroll 联动暂不强行接入本周 MVP，先把交接准备和 handoff 信息收口。
- 面试反馈支持草稿即回写主流程，提交后再作为正式归档，兼顾演示效率和真实流程。

## 联调与验证

- `python3 -m unittest discover -s tests` 已扩展到 `23` 条测试并全部通过。
- `python3 -m compileall aihr scripts tests` 通过。
- 本地站点已重新迁移并可访问：`http://development.localhost:18000`
- demo 数据已成功灌入，当前可直接看到：
  - `HR-HIREQ-00001`
  - `HR-OPN-2026-0001`
  - `HR-INT-2026-0001`
  - `HR-INT-FEED-0001`
  - `HR-OFF-2026-00001`
  - `HR-EMP-ONB-2026-00001`
- 使用 Playwright 做了真实浏览器验证：
  - 登录页显示为 `AIHR 登录`
  - 顶部导航品牌已显示 `AIHR`
  - 登录后会稳定进入可用的 `AIHR 招聘总览` 路由，不再落到不存在的中文 slug
  - 左侧导航只显示 AIHR 工作台
  - 角色工作台可切换到 `AIHR 用人经理中心`、`AIHR 面试协同中心`
  - `AIHR 用人经理中心` 中 `待经理复核` 已显示 `3 条待复核`
  - 游客访问 `/projects` 会自动跳到 `AIHR 登录`
  - 已登录后手动访问 `/projects` 会自动回到 `AIHR 招聘总览`
  - `python3 -m unittest discover -s tests` 仍保持通过，国际化辅助逻辑已加入测试
  - 浏览器实测 `AI Screening` 表单中已出现：
    - `初筛详情`
    - `候选人档案`
    - `招聘中岗位`
    - `状态`
    - `总评分`
    - `匹配技能`
  - 浏览器实测 `AI Screening` 列表页已显示：
    - `AI 初筛`
    - `列表视图`
    - `添加 AI 初筛`
    - `过滤条件`
    - `已分配给`
    - `创建人`
    - `最后更新时间`
  - 浏览器实测 `Job Requisition` 列表页已显示：
    - `岗位需求单`
    - `新增岗位需求单`
    - `职位`
    - `需求提出人`
    - `最后更新时间`
  - 浏览器实测 `新建岗位需求单` 表单页已显示：
    - `新建 岗位需求单`
    - `尚未保存`
    - `保存`
    - `详细信息`
    - `职位描述`
    - `编号规则`
    - `招聘人数`
    - `预期薪酬`
    - `需求提出人`
    - `时间安排`
    - `发布日期`
    - `期望到岗日期`
  - 面试页能看到 `AIHR 面试概览`
  - Offer 页能看到 `AIHR 录用概览`
  - 面试反馈页能看到 `AIHR 反馈概览`
  - 入职交接页能看到 `AIHR 入职概览`
  - `准备入职交接`、`生成面试官资料包` 动作均能执行

## 发现并解决的问题

- `prepare_job_offer_handoff` / `prepare_interviewer_pack` 在按钮调用时出现 `TimestampMismatchError`。
- 根因是 `validate` 钩子已自动回写候选人状态，API 又额外保存了一次候选人。
- 处理方式：
  - 保留 `validate` 钩子作为统一状态同步入口
  - 去掉 API 内重复的候选人保存
- 重灌 demo 数据时，原生 `Interview.Scheduled On` 因只写一次约束无法直接改动。
- 处理方式：
  - seed 逻辑改为“老记录不强改排期，只补必要字段”
  - 面试反馈提交失败时保留草稿，避免整批 demo 灌数回滚
- 命令栏和搜索栏仍残留旧工作台历史，如 `AIHR 招聘作战台`、`AIHR 面试官台`，且会生成已过时的入口。
- 根因是 `Route History` 仍保留旧路由记录，前端 `recent/frequent links` 会继续复用这些历史。
- 处理方式：
  - 新增 `aihr.setup.navigation`，把旧工作台历史统一改写到当前命名
  - 登录加载时通过 `extend_bootinfo` 自动清洗 `frequently_visited_links`
  - 前端 `aihr_desk.js` 增加命令栏历史净化，过滤所有 `Workspaces/*` 频繁访问项
  - 直接执行了一次数据库级 `cleanup_route_history`，确认 `Workspaces/HR` 已清空，旧 `AIHR 招聘作战台 / 用人经理台 / 面试官台` 已全部改写成新命名
- 右上角工作台切换和旧中文工作台 slug 仍会落到 `/app/aihr-招聘总览` 一类地址，并弹出“未找到页面”。
- 根因是 Frappe 前端会在部分入口里按中文工作台标题生成 slug，而直接打开这些旧中文 slug 时，服务端此前没有做兼容归一。
- 处理方式：
  - `aihr_desk.js` 为 `AIHR 招聘总览 / 用人经理中心 / 面试协同中心` 注册中文 slug 别名，并统一重写到稳定英文路由
  - 新增 `before_request` 级别的 desk 路径归一化，对 `/app` 和旧中文 slug 统一返回 `308` 到稳定工作台地址
  - 浏览器实测直接访问 `/app/aihr-招聘总览` 已会自动落到 `/app/aihr-hiring-hq`，不再弹“未找到页面”
- 登录后直接打开站点根路径或网站侧个人中心时，仍会出现 ERP/门户风格的“我的账户”页面，与 AIHR 主流程无关，容易让业务方误以为这是系统默认首页。
- 根因是 Frappe/ERPNext 自带网站门户的 `/me` 页面仍然可达，右上角用户菜单里原生也保留了 `查看网站`。
- 处理方式：
  - 对登录态的 `/me` 和门户别名统一重定向到 `AIHR 招聘总览`
  - 保留游客访问 `/` 时的登录页体验，但登录后网站侧会被前端脚本立即送回 Desk
  - Desk 右上角用户菜单移除 `查看网站`，并在前端兜底覆盖 `view_website` 动作，避免再次跳回门户页
- 左侧 sidebar 数据并未丢失，但默认 Frappe 工作台侧栏在 AIHR 视觉体系下显得过窄、过淡，远看会像空白区。
- 浏览器实测当前侧栏实际已加载 3 个工作台入口：
  - `AIHR 招聘总览`
  - `AIHR 面试协同中心`
  - `AIHR 用人经理中心`
- 处理方式：
  - 加强侧栏容器宽度、分组标题对比度、项目高度与文字可读性
  - 将 `Public` 分组名明确显示为 `AIHR 导航`
  - 隐藏工作台编辑拖拽控制，减少左侧噪音，让它更像正式导航栏
- 左侧顶部仍有一条看起来像“空白占位”的白框。
- 根因是 Frappe 工作台在只有一个可见分组时，仍然保留了分组标题按钮；在 AIHR 侧栏里这块既没有信息量，也会被误认为是空白输入框。
- 处理方式：
  - 当侧栏只剩一个可见分组时，自动隐藏该分组标题按钮
  - 左侧导航直接从第一个工作台入口开始显示，减少误解
- 用户浏览器里随后仍出现白框且 AIHR 工作台入口偶发不显示。
- 根因进一步确认是 Frappe 原生 workspace sidebar 在不同渲染时序下仍会残留占位结构，单纯隐藏分组标题不够稳。
- 处理方式：
  - AIHR 首页工作台左栏改成自定义固定导航
  - 仅保留 `AIHR 招聘总览`、`AIHR 用人经理中心`、`AIHR 面试协同中心` 三个入口
  - 不再依赖原生 workspace sidebar 的分组标题、翻译时序和编辑态控制
  - 对原生 sidebar 后续重新插回的节点做强制隐藏，只保留 AIHR 自定义导航
  - 对重绘后再次插回的旧节点增加内联隐藏兜底，避免浏览器端仍显示白框或旧工作台列表
- 上游 `user-profile / leaderboard` 页面仍会暴露 `能量点 / 审核点 / 排名 / 月度排名 / 排行榜` 这类通用人物画像能力，与 AIHR 招聘产品定位不符。
- 根因是这些页面来自 Frappe 原生人物页，不属于 AIHR 自定义业务模块。
- 处理方式：
  - 在服务端路由归一化中，新增 `/app/user-profile*`、`/app/leaderboard*` 到 `AIHR 招聘总览` 的收口
  - 在 AIHR Desk 用户菜单净化中移除 `My Profile / User Profile / Leaderboard / 排行榜`
  - 明确产品原则：AIHR 不做公司内部排行榜，如需评估，优先做供应商交付质量
- 供应商简历更常通过线下 ZIP 包提供，之前系统只适合单份简历录入或单个附件解析，不符合真实业务操作习惯。
- 处理方式：
  - 新增 `aihr.services.resume_intake`，支持 ZIP 压缩包批量解压与逐份解析
  - `resume_parser` 增强支持 `DOCX` 正文提取，并对 `DOC` 增加 best-effort 解析
  - `Job Opening` 页面新增 `导入简历压缩包` 动作，支持：
    - 上传 ZIP 包
    - 填写供应商名称
    - 填写来源渠道
    - 选择导入后自动生成 AI 摘要
  - 导入后自动创建或更新 `Job Applicant`，并记录：
    - 原始文件名
    - 供应商
    - 来源渠道
    - 导入批次
    - 解析状态
  - 当前摘要仍以系统内第一版 AI 初筛逻辑为主，尚未切换到标准化在线大模型摘要服务
- 登录页原有 OEM 版式结构不稳定，标题、表单和说明卡片在大屏下容易形成三块漂浮布局，视觉重心不统一。
- 处理方式：
  - 重组登录页 DOM，把品牌引导和登录表单合并为左侧一体化面板
  - 右侧保留单独产品说明卡，减少口号感，强化产品边界和业务语义
  - 登录页文字从“品牌后台宣言”调整为更克制的招聘产品说明
  - 统一表单卡片、按钮、输入框、背景和双栏比例，让登录页更像正式产品入口而不是演示页
- 进一步打磨登录页桌面布局：
  - 桌面端改为明确的 `50/50` 双栏结构
  - 左右两侧都收口为完整卡片，减少漂浮感
  - 左侧登录面板与右侧说明卡统一高度和视觉重量
  - 窄屏时再回落为上下堆叠，保持移动端可用性
- 登录页更新后，业务侧浏览器仍持续命中旧版 `aihr_login.css / aihr_login.js`，导致界面长期停留在早期海报版布局。
- 根因是登录页 Web 资源此前使用固定路径，浏览器会长时间复用旧缓存。
- 处理方式：
  - 为登录页 CSS / JS 增加资源版本号
  - 重启本地 Frappe 开发服务，让新 hooks 生效
  - 站点现在已输出带版本号的新登录资源，后续刷新会直接命中新版界面
- 登录页在拿到新版资源后，桌面端仍出现“左侧登录卡在上、右侧说明卡在下”的异常堆叠。
- 根因是登录页 DOM 重组时，把标题和表单移动到了新容器里，但原始 `login-content page-card` 空壳仍然留在 `section.for-login` 里，导致 grid 实际有 3 个子节点：
  - 左侧登录面板
  - 空白旧卡片
  - 右侧说明卡
- 处理方式：
  - 改为把整个 `login-content page-card` 一并移动进左侧面板
  - 保证 `section.for-login` 最终只剩 2 个子节点
  - 实测桌面端 `1728px` 宽度下已恢复正常左右双栏布局
- AIHR 后台首页在用户常用浏览器窗口里出现整页黑底，搜索栏也跟着发黑，和当前浅色品牌风格不一致。
- 核查结果：
  - 真实页面计算样式已经是浅色，Playwright 最新截图也为浅色
  - 问题主要来自后台 desk 资源在常用窗口里持续命中旧缓存，而非 Docker 容器本身异常
  - 同时 AIHR desk 对外层容器和搜索栏的浅色覆盖还不够强，暗色残留时观感会明显变差
- 处理方式：
  - 将后台入口资源切换为新文件名 `aihr_desk_v2.css / aihr_desk_v2.js`，绕开旧缓存
  - 强化 `body / #body / page-container / layout-main / search-bar` 的浅色覆盖和 `color-scheme: light`
  - 重启本地 Frappe 开发服务并重新 `build + clear-cache`
  - 实测后台当前已输出新版 desk 资源，页面恢复为统一浅色背景
- 登录页“通过邮件链接登录”点击后仍然出现上下堆叠，不同登录入口没有保持统一布局。
- 根因有两层：
  - 登录页 CSS 只覆盖了 `for-login / for-email-login`，没有覆盖 `for-login-with-email-link / for-forgot`
  - Frappe 自带登录脚本在切换路由时会对可见 section 直接写入 `display: block`，把 AIHR 的 `display: grid` 覆盖掉
- 处理方式：
  - 扩展登录页 CSS，让 `for-login / for-email-login / for-login-with-email-link / for-forgot` 全部使用同一套双栏规则
  - 将登录页文案改为配置驱动，不再依赖零散条件判断，统一维护标题和副标题
  - 新增 section 布局同步逻辑：只要 Frappe 把某个登录区块切成可见，AIHR 就立即把它修正回 `grid`
  - 再次提升登录页资源版本号到 `v5`，确保浏览器强制拿到最新登录资源
  - 实测 `登录 / 通过邮件链接登录 / 忘记密码` 三个入口都已恢复为稳定双栏布局

## 当前已知问题

- 本地仍然有 `socket.io 404` 控制台报错，因为实时服务层没有一起拉起。
- 该问题不影响当前 MVP 的页面录入、按钮动作和演示流程。
- 员工主档自动创建和 Payroll 正式联动还未完成。
- 富文本编辑器工具条和部分屏幕阅读辅助文案仍有零散英文，不影响主流程可见 UI，但还没有达到“全站无英文残留”。
- 当前 ZIP 批量导入已可用，但还没有独立的导入结果页、失败日志下载和供应商质量看板。
- 当前 AI 摘要已能自动生成，但仍主要依赖系统内本地规则和启发式抽取，尚未接入统一的大模型摘要工作流。

## 下一步

- 继续推进中文优先的表单标签和 Workspace 文案，并向富文本工具条、提示文案和帮助文本继续收口。
- 继续完善 ZIP 简历批量导入的导入结果展示、失败日志和去重策略。
- 规划供应商交付质量视图，不做内部排行榜。
- 评估并接入标准化大模型摘要服务，统一候选人摘要口径。
- 补 `Employee` 主档自动创建入口，衔接 `Employee Onboarding`。
- 继续把经理/面试官的角色工作台做得更轻，减少无关入口与字段暴露。
- 视时间评估是否顺手接入更轻量的实时服务或屏蔽本地 `socket.io` 噪音。
