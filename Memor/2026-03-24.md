# 2026-03-24

## 今日进展
- 收口部门经理新建岗位需求单的首屏体验，避免“需求提出人”和“职位描述”成为理解门槛。
- 新增 `Job Requisition.aihr_role_description_input`，作为经理侧首屏可见的“岗位职责与要求”输入框，并与标准 `description` 保持同步。
- 经理视角下隐藏原始 `requested_by`、`description` 和请求人明细区块，改为顶部说明文案：需求提出人自动带入当前登录经理。
- 经理保存前会优先校验“岗位职责与要求”，不再让用户先撞到原始的缺字段提示。

## 关键验证
- `python3 -m unittest discover -s tests` 通过，当前 90 条。
- `python3 -m compileall aihr tests` 通过。
- 已执行站点 `migrate + clear-cache + build`。
- 真实浏览器验证：交付中心经理新建岗位需求单时，首屏已出现“岗位职责与要求 *”输入区，顶部提示会明确说明需求提出人自动带入。

## 当前判断
- 岗位需求单现在更接近“部门经理填写业务内容”的心智模型，而不是让经理理解 Frappe 主数据。
- 这属于招聘主链路的必要收口，改动保持在轻量兼容范围内，没有扩展额外 HR 模块。

## 下一步
- 继续按招聘 MVP 主线验收：部门经理提需求 -> HR 创建招聘中岗位 -> ZIP 导入 -> AI 初筛 -> 经理复核 -> 面试推进。
